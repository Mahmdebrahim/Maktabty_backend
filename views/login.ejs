 <!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>create a new user</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: #f8fafc;
      color: #1e293b;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
      padding: 20px;
    }
    .card {
      background: white;
      padding: 40px 35px;
      border-radius: 18px;
      box-shadow: 0 10px 35px rgba(0,0,0,0.1);
      width: 100%;
      max-width: 480px;
    }
    h1 {
      text-align: center;
      color: #4f46e5;
      margin-bottom: 30px;
      font-size: 2rem;
    }
    input {
      width: 100%;
      padding: 14px;
      margin: 10px 0;
      border: 2px solid #e2e8f0;
      border-radius: 12px;
      font-size: 1.1rem;
    }
    input:focus {
      outline: none;
      border-color: #4f46e5;
    }
    button {
      width: 100%;
      padding: 16px;
      margin-top: 15px;
      background: #4f46e5;
      color: white;
      border: none;
      border-radius: 12px;
      font-size: 1.2rem;
      font-weight: bold;
      cursor: pointer;
    }
    button:hover {
      background: #4338ca;
    }
    .back {
      display: block;
      text-align: center;
      margin-top: 20px;
      color: #10b981;
      text-decoration: none;
      font-weight: 600;
    }
    .back:hover { color: #059669; }
  </style>
</head>
<body>
  <div class="card">
    <h1>Login Page</h1>
    <form action="/users/login" method="POST">
      <input type="text" name="username" placeholder="username">
      <input type="password" name="password" placeholder="password" min="1">
      <button type="submit">login</button>
    </form>
    <a href="/users/new/user" class="back">register</a>
  </div>
</body>
</html>